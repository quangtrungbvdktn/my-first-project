# AI Video Translator & Dubbing Studio

A Windows-oriented desktop application for translating and dubbing video.

## Development

```bash
python -m pip install -e ".[dev]"
python -m ruff check .
python -m pytest -q
```
